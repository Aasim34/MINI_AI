# Generated Projects

This folder contains all projects generated by the AI Full Stack Generator.

## 📂 Projects

### dog-walking-marketplace
- **Description:** Dog Walking and Pet Sitting Marketplace with GPS tracking and reviews
- **Status:** ✅ Complete
- **Framework:** Next.js 14 + React 18 + Tailwind CSS
- **Features:**
  - Walker profile browsing
  - Real-time booking system
  - GPS tracking for active walks
  - Review and rating system
  - Responsive design

**Quick Start:**
```bash
cd dog-walking-marketplace
npm install
npm run dev
```

Visit `http://localhost:3000`

---

## 🚀 Generating New Projects

To create a new project, run the generator from the parent directory:

```bash
cd ..
$env:OPENAI_API_KEY = "your-api-key"
python ai_fullstack_generator.py
```

The new project will be automatically created in this `Projects/` folder.

---

## 📋 Project Template Structure

Each generated project includes:

```
project-name/
├── src/
│   ├── pages/              # Next.js pages & API routes
│   ├── components/         # React components
│   ├── styles/             # Tailwind CSS
│   └── api/                # Backend API endpoints
├── public/                 # Static assets
├── package.json            # Dependencies
├── next.config.js          # Next.js configuration
├── tailwind.config.js      # Tailwind CSS config
├── jsconfig.json           # Path aliases
└── README.md               # Project documentation
```

---

## 🔧 Customization

Each project can be customized:

1. **Styling:** Modify `tailwind.config.js` and `src/styles/globals.css`
2. **Pages:** Edit files in `src/pages/`
3. **Components:** Create/edit files in `src/components/`
4. **API Routes:** Modify `src/pages/api/` endpoints

---

## 📦 Deployment

### Deploy to Vercel (Recommended for Next.js)

```bash
npm install -g vercel
vercel
```

### Deploy to Other Platforms

- **Netlify:** Use `npm run build` output
- **AWS Amplify:** Connect GitHub repo
- **Railway:** `railway up`
- **Heroku:** `git push heroku main`

---

## 🔐 Environment Variables

Create `.env.local` in the project root:

```
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_GOOGLE_MAPS_KEY=your-key
DATABASE_URL=your-db-url
JWT_SECRET=your-secret
```

---

## 📚 Resources

- [Next.js Documentation](https://nextjs.org/docs)
- [React Documentation](https://react.dev)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [Backend API Guide](../backend/README.md)

---

**Generated with AI Full Stack Generator** 🤖
